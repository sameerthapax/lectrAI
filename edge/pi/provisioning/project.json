{
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "name": "pi-provision",
  "root": "edge/pi/provisioning",
  "targets": {
    "install": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bash edge/pi/provisioning/install.sh"
      }
    }
  }
}
